{
  "name": "astrophysicist-portfolio",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build --outDir docs",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && git add docs && git commit -m \"Build for GitHub Pages\" && git push origin main"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^5.2.5",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
